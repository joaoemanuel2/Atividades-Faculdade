#include <iostream>
using namespace std;

int main()
{
    int x;
    int i = 1;

    cin >> x;
    while (i <= x)
    {
        if (x % i == 0)
        {
            cout << i << " ";
        }
        i++;
    }
    /*  for (int i = 1; i <= x; i = i + 1)
        {
            if (x % i == 0)
            {
                cout << " " << i;
            }
        }*/
    return 0;
}
while (X >= 0)
{
    resultado = pow(X, Y);
    cout << fixed << setprecision(4) << resultado << endl;
    break;
}
